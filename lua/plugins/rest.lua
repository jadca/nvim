return {
    --{
        --'rest-nvim/rest.nvim',
        --dependencies = { 'nvim-lua/plenary.nvim' },  -- Asegúrate de que las dependencias están aquí
        --opts = {
            --rocks = {
                --hererocks = true,   -- Activa hererocks para las dependencias
                --enabled = true      -- Habilita el soporte de rocks en general
            --},
        --},
        --config = function()
            --require('rest-nvim').setup()
        --end,
    --}
}
